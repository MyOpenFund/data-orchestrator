# Local ingestion state (resume ledgers) — do not commit.
state/

# Per-machine secrets / paths - do not commit. Use .env.example as a template.
.env
.env.local

__pycache__/
*.egg-info/
build/
dist/
.venv/
venv/

# macOS
.DS_Store

# superpowers process artifacts (specs/plans) - live on the `documentation`
# branch, never on main.
.superpowers/
docs/superpowers/
